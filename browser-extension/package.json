{
  "name": "wishdeck-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Browser extension for adding items to WishDeck.",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist"
  }
}
