fastapi==0.141.1
uvicorn[standard]==0.53.0
sqlalchemy[asyncio]==2.0.54
alembic==1.20.0
aiosqlite==0.22.1
# PostgreSQL driver. The app is async (SQLAlchemy 2.0 asyncio), but a sync
# driver works under the async engine via its thread pool. Use
# `postgresql+asyncpg://` + `asyncpg` instead if you want the native async
# driver (see commented line below).
psycopg2-binary==2.9.13
# asyncpg==0.31.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.32
httpx==0.28.1
beautifulsoup4==4.15.0
python-jose[cryptography]==3.5.0
passlib[argon2]==1.7.4
argon2-cffi==25.1.0
authlib==1.8.0
