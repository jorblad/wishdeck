fastapi==0.141.1
uvicorn[standard]==0.54.0
sqlalchemy[asyncio]==2.1.1
alembic==1.20.0
aiosqlite==0.22.1
# Async-native PostgreSQL driver (psycopg 3). SQLAlchemy's asyncio engine
# requires an async DBAPI; psycopg3 is used via the `postgresql+psycopg://`
# dialect. A bare CNPG `postgresql://` URI is rewritten to that dialect in
# app/core/config.py, so the operator-provided secret can be used as-is.
psycopg[binary]==3.3.6
pydantic==2.13.5
pydantic-settings==2.15.0
python-multipart==0.0.32
httpx2==2.13.1
beautifulsoup4==4.15.0
python-jose[cryptography]==3.5.0
passlib[argon2]==1.7.4
argon2-cffi==25.1.0
